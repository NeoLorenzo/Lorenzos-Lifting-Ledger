{
  "name": "lorenzos-lifting-ledger",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/dev-server.mjs",
    "check:syntax": "node --check app.js && node --check analytics.js && node --check body-weight.js && node --check features/body-weight.js && node --check literature.js && node --check presets.js && node --check relative-e1rm.js && node --check service-worker.js && node --check scripts/dev-server.mjs",
    "agent:check": "node scripts/agent-check.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
